// Bu senin Prisma şema dosyan,
// bu hakkında daha fazla bilgiyi dokümantasyonda bulabilirsin: https://pris.ly/d/prisma-schema

// Sorgularını hızlandırmak için veya sunucu fonksiyonlarıyla veya kenar işlevleriyle kolayca ölçeklendirmek için yollar mı arıyorsun?
// Prisma Accelerate'ı dene: https://pris.ly/cli/accelerate-init

// Prisma tarafından üretilen kodları sağlayan generator belirtilir.

generator client {
  provider = "prisma-client-js"
}

// Veritabanı bağlantısını belirtiyoruz, burada PostgreSQL kullanıyoruz.

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Kullanıcı modelini tanımlıyoruz. Bu model, kullanıcıları temsil edecek ve veritabanında bir tabloya karşılık gelecek.

model User {
  id           Int      @id @default(autoincrement())
  clerkId      String   @unique
  name         String?
  email        String   @unique
  profileImage String?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}
